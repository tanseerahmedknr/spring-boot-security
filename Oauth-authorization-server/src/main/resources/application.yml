server:
  port: 9000

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/user_registration
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
